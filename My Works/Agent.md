# Agent Instructions — My Works

**Scope**: records of **specific, named work projects** ONLY (Zihan's day job). NOT general learning/research (→ `My Notes/`), NOT personal idea ventures (→ `My Creations/`).

## What lives here

```
My works root.md      ← index
IntelProjects/          gitignored (see below). Work/company-sensitive notes, debug transcripts.
```

`LLM/` used to live here but has been moved to `My Notes/` — this module is now scoped down to work-project records only. Don't re-add general LLM/tech learning content here even if it feels adjacent to work; it belongs in `My Notes/` instead.

## `IntelProjects/` is sensitive

`.gitignore` at vault root excludes `*/IntelProjects/` anywhere in the tree — content here is intentionally untracked by git. Treat anything under this folder as private/work-sensitive; don't assume it's safe to reference, quote, or surface outside this vault.

## Note conventions (same as rest of vault)

- Internal links use wiki-link syntax `[[Note Name]]`, not markdown `[text](path)`.
- Mixed Chinese/English is normal.
- `==text==` for inline highlight emphasis.
