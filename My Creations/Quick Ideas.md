### For LLM Agent Helpers:

This is a running capture list of Zihan's spontaneous ideas ("灵机一动") — candidates for future projects under `My Creations/`, **not yet committed to**.

- This is a *capture-only* list — much lighter-weight than `Quicknotes.md`. When Zihan drops a new idea here, just add it as a checklist item in his own words. Don't research it, don't expand/refine it, don't start building anything unprompted.
- Only when Zihan explicitly asks to move forward with a specific idea: create its own subfolder `My Creations/<Idea Name>/` and start the idea → research → PRD/方案 → implementation pipeline documented in this folder's `Agent.md`. The first file in that subfolder (`idea.md`) is where the raw spark gets formalized — the one-liner below is just the seed.
- Check the box (`- [x]`) here once an idea has been promoted into its own subfolder, and note where it landed (e.g. `→ My Creations/<Idea Name>/`).
- An idea can sit unchecked here indefinitely — that's fine, this list is not a backlog that needs clearing.

### Idea List

- [ ]
